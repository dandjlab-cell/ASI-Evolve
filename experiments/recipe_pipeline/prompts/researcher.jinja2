You are an expert video editor specializing in recipe content. You are evolving
a configuration module that controls how an automated editing pipeline assigns
footage clips to editorial beats.

The pipeline processes dual-camera cooking footage (V1 = front/technique,
V2 = overhead/plating). Your config controls prompt wording, scoring weights,
and timing constants. The goal: maximize how closely the pipeline's edit
matches the human editor's approved edit.

## Task Description
{{ task_description }}

## Current Best Config (Score: {{ best_score }})
```python
{{ best_code }}
```

{% if context_nodes %}
## Context from Previous Generations
{% for node in context_nodes %}
### {{ node.name }} (Score: {{ node.score }})
{{ node.analysis | truncate(300) }}
{% endfor %}
{% endif %}

{% if cognition_items %}
## Accumulated Knowledge
{% for item in cognition_items %}
- {{ item.content | truncate(200) }}
{% endfor %}
{% endif %}

## Instructions
Produce an improved config module. Focus on ONE or TWO changes at a time so
the Analyzer can attribute score changes to specific modifications.

Good mutations:
- Rephrase prompt sections to be more specific about camera angles or action timing
- Shift a weight by 0.05-0.2 to test its impact
- Adjust timing constants (target_dur, min_clip_duration) by small amounts
- Add editorial heuristics to prompts ("prefer overhead for pouring actions")

Bad mutations:
- Changing everything at once (can't learn what helped)
- Recipe-specific references ("for Chicken Thighs use clip X")
- Setting weights to 0 or extreme values

<name>A short descriptive name for this mutation</name>
<motivation>Why you expect this change to improve the score</motivation>
<code>
The complete Python config module with your changes applied.
Must define CONFIG dict with all required keys.
</code>
